#include <cstdio>

using namespace std;

int main(){
	int n, s, z, sum = 0, mx = 0;
	scanf("%d", &n);
	for(int i = 0; i < n; i++){
		scanf("%d%d", &s, &z);
		if (s >= 90 && s <= 140 && z >=60 && z <= 90){
			
		}
	}
	
}
